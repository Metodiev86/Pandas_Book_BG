# 1.	Четене от уебсайт: Отворете уебсайт с таблични данни (например, списък с най-високите сгради в света от Wikipedia). Копирайте част от таблицата (няколко реда и колони) в клипборда. След това използвайте pd.read_clipboard() в Pandas, за да прочетете данните в DataFrame. Изведете DataFrame-а и проверете неговата структура.

import pandas as pd

# Стъпка 1: Уверете се, че в клипборда има таблични данни (например, от Wikipedia таблица)
# Например: https://en.wikipedia.org/wiki/List_of_tallest_buildings

# Стъпка 2: Четене от клипборда (използва по подразбиране табулация като разделител)
try:
    df = pd.read_clipboard()
    print("Таблица, прочетена от клипборда:\n")
    print(df.head())  # Преглед на първите 5 реда
    print("\nИнформация за DataFrame:")
    print(df.info())
except Exception as e:
    print(f"Възникна грешка при четене от клипборда: {e}")
