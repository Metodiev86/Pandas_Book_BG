# 5.	Запис в LaTeX файл с UTF-8 кодиране: Създайте DataFrame, който съдържа български символи в една от колоните. Запишете го в LaTeX файл, като изрично укажете кодиране 'utf-8'. След това създайте прост LaTeX документ, който включва тази таблица, компилирайте го и се уверете, че българските символи се показват правилно. (За тази задача ще ви е необходима работеща LaTeX инсталация).

import pandas as pd

# Създаваме DataFrame
df = pd.DataFrame({
    "Продукт": ["Ябълка", "Круша", "Грозде"],
    "Количество": [15, 25, 35]
})

# Генерираме LaTeX кода
latex_code = df.to_latex(index=False)

# Записваме във файл
with open("bulgarian_table.tex", "w", encoding="utf-8") as f:
    f.write(latex_code)

print("Файлът 'bulgarian_table.tex' беше записан успешно.")



#=============================================Компилиране:=====================================================
# Можеш да компилираш main.tex с:
#
# bash:
# pdflatex main.tex
# ==============================================================================================================
# Ако използваш TeXstudio или Overleaf – просто зареди main.tex и добави products_table.tex в проекта.
# ==============================================================================================================