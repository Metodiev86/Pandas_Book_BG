# Запис с различно кодиране: Създайте DataFrame с текст, съдържащ символи, които не са ASCII (например, букви на кирилица). Запишете DataFrame-а в CSV файл bulgarian.csv, като използвате кодиране 'utf-8'. След това прочетете файла отново с 'utf-8' кодиране и изведете съдържанието, за да се уверите, че данните са прочетени правилно. Опитайте да прочетете същия файл без да указвате кодиране и вижте дали ще има проблеми.

import pandas as pd

# Създаваме DataFrame с текст на кирилица
data = {
    'Име': ['Алиса', 'Борис', 'Веселин'],
    'Град': ['София', 'Пловдив', 'Варна'],
    'Забележка': ['Пример №1', 'Тест 🧪', 'Данни с юникод 😊']
}

df = pd.DataFrame(data)

# === 1. Записваме CSV файла с utf-8 кодиране ===
df.to_csv('bulgarian.csv', encoding='utf-8', index=False)

# === 2. Прочитаме файла с указано utf-8 кодиране ===
df_utf8 = pd.read_csv('bulgarian.csv', encoding='utf-8')

print("Прочетен DataFrame с utf-8 кодиране:\n")
print(df_utf8)

# === 3. Опит за четене без указано кодиране (може да даде грешка на някои системи) ===
try:
    df_default = pd.read_csv('bulgarian.csv')  # encoding по подразбиране
    print("\nПрочетен DataFrame без указано кодиране:\n")
    print(df_default)
except UnicodeDecodeError as e:
    print("\n⚠️ Грешка при четене без кодиране (encoding по подразбиране):")
    print(e)

#=======================================================================================================================
# Обяснение:
# encoding='utf-8' гарантира, че всички юникод символи (напр. кирилица, емоджита) се запазват правилно;
#
# Ако системният encoding не е utf-8, при четене без encoding може да се получи UnicodeDecodeError;
#
# index=False – не записваме индекс в CSV, за по-чист изход.

#=======================================================================================================================
#
# Ако системата не поддържа utf-8 по подразбиране:
#
# ⚠️ Грешка при четене без кодиране (encoding по подразбиране):
# 'utf-8' codec can't decode byte 0xd0 in position 0: invalid continuation byte